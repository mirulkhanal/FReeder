diff --git a/node_modules/react-native-readium/android/build.gradle b/node_modules/react-native-readium/android/build.gradle
index 5ed5f9b..960bf01 100644
--- a/node_modules/react-native-readium/android/build.gradle
+++ b/node_modules/react-native-readium/android/build.gradle
@@ -27,6 +27,11 @@ def getExtOrIntegerDefault(name) {
 }
 
 android {
+  // Use the host app NDK so AGP does not download a second default package
+  // (27.0.x) that often fails unzipping in CI.
+  ndkVersion rootProject.ext.has('ndkVersion')
+      ? rootProject.ext.get('ndkVersion')
+      : "27.1.12297006"
   compileSdkVersion getExtOrIntegerDefault('compileSdkVersion')
   namespace "com.reactnativereadium"
   defaultConfig {
@@ -39,7 +44,9 @@ android {
       cmake {
         cppFlags "-O2 -frtti -fexceptions -Wall -fstack-protector-all"
         arguments "-DANDROID_STL=c++_shared", "-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON"
-        abiFilters "x86", "x86_64", "armeabi-v7a", "arm64-v8a"
+        // Respect app ABI filters; hardcoded all-ABI list breaks Nitro linking
+        // when nitro-modules is only built for arm64-v8a (see readium#117).
+        abiFilters "arm64-v8a"
       }
     }
   }
diff --git a/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/HybridReadiumView.kt b/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/HybridReadiumView.kt
index 3744f66..14cd81d 100644
--- a/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/HybridReadiumView.kt
+++ b/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/HybridReadiumView.kt
@@ -275,6 +275,14 @@ class HybridReadiumView(private val context: android.content.Context) : HybridRe
       }
     }
 
+    // Apply preferences BEFORE committing so the navigator factory receives
+    // scroll/columnCount on first load (not only via a late submitPreferences).
+    preferences?.let { prefs ->
+      if (frag is EpubReaderFragment) {
+        frag.updatePreferences(nitroPreferencesToEpub(prefs))
+      }
+    }
+
     activity.supportFragmentManager
       .beginTransaction()
       .replace(hostView.id, frag, hostView.id.toString())
diff --git a/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/reader/EpubReaderFragment.kt b/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/reader/EpubReaderFragment.kt
index 5f34c4e..c65bec0 100644
--- a/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/reader/EpubReaderFragment.kt
+++ b/node_modules/react-native-readium/android/src/main/java/com/reactnativereadium/reader/EpubReaderFragment.kt
@@ -167,12 +167,12 @@ class EpubReaderFragment : VisualReaderFragment() {
         val am = activity.getSystemService(AppCompatActivity.ACCESSIBILITY_SERVICE) as AccessibilityManager
         isExploreByTouchEnabled = am.isTouchExplorationEnabled
 
-        userPreferences = if (isExploreByTouchEnabled) {
-            userPreferences.plus(EpubPreferences(scroll = true))
-        } else {
-            userPreferences.plus(EpubPreferences(scroll = null))
+        if (isExploreByTouchEnabled) {
+            userPreferences = userPreferences.plus(EpubPreferences(scroll = true))
+            (navigator as? EpubNavigatorFragment)?.submitPreferences(userPreferences)
+        } else if (this::navigator.isInitialized) {
+            (navigator as? EpubNavigatorFragment)?.submitPreferences(userPreferences)
         }
-        (navigator as? EpubNavigatorFragment)?.submitPreferences(userPreferences)
     }
 
     private inner class SelectionActionModeCallback : ActionMode.Callback {
